# Auto-generated by bin/generate_sdks.rb - DO NOT EDIT

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "aho-sdk"
version = "0.1.1"
description = "Python SDK for the Aho Verifiable Credentials API"
readme = "README.md"
license = {text = "MIT"}
authors = [
    {name = "Aho", email = "support@aho.com"}
]
requires-python = ">=3.9"
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "typing_extensions>=4.0",
]

[project.urls]
Homepage = "https://api.aho.com"
Documentation = "https://api.aho.com/developers/python-sdk"
Repository = "https://github.com/aho-hq/aho-python"
Changelog = "https://github.com/aho-hq/aho-python/blob/main/CHANGELOG.md"

[project.optional-dependencies]
async = [
    "httpx>=0.25.0",
]
validation = [
    "pydantic>=2.0",
]
all = [
    "httpx>=0.25.0",
    "pydantic>=2.0",
]
dev = [
    "pytest>=7.0",
    "pytest-cov>=4.0",
    "pytest-asyncio>=0.21",
    "responses>=0.23",
    "httpx>=0.25.0",
    "mypy>=1.0",
    "ruff>=0.1",
    "pydantic>=2.0",
]

[tool.setuptools.packages.find]
include = ["aho_sdk*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_functions = ["test_*"]

[tool.mypy]
python_version = "3.9"
strict = true
warn_return_any = true
warn_unused_ignores = true

[tool.ruff]
target-version = "py39"
line-length = 100
select = ["E", "F", "I", "N", "W", "UP"]
